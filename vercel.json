{
  "buildCommand": "echo 'Skipping build - API only deployment'",
  "outputDirectory": ".",
  "installCommand": "npm install",
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}